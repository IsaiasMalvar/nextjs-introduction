import { Hurricane, Inter, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const hurricane = Hurricane({
  subsets: ['latin'],
  weight: '400',
});

export const lusitana = Lusitana({ weight: '400', subsets: ['latin'] });
